# Status: in-flight build

This directory holds the SPEC and PLAN for the shared Platform layer (the foundation underneath Vault / PII Frisk / Healer) — no shipped code lives here.
The Platform module is **not part of the published `xlsx-for-ai` npm package surface**; its implementation lives in the private `xlsx-for-ai-server` repo at `src/lib/platform/`.

For scope and design see `PLAN.md` in this directory.
