# Status: in-flight build

This directory holds the SPEC and PLAN for Vault — no shipped code lives here.
Vault is **not part of the published `xlsx-for-ai` npm package surface**; it is a server-side sub-product whose implementation lives in the private `xlsx-for-ai-server` repo.

For scope and design see `PLAN.md` in this directory.
